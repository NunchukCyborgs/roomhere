import { Component } from '@angular/core';

@Component({
  selector: 'roomhere-logo',
  styles: [require('./styles.scss').toString()],
  template: require('./template.html').toString(),
})
export class Logo {
}
